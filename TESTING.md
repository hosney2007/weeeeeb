# تشغيل الاختبارات (Tests)

```bash
pip install -r requirements-dev.txt
pytest
```

الاختبارات بتستخدم قاعدة بيانات SQLite مؤقتة (بتتعمل وتتمسح تلقائي) عشان محدش
يلمس قاعدة البيانات الحقيقية بتاعتك. الـ CSRF protection والـ rate limiting
متلغيين تلقائيًا وقت تشغيل الاختبارات فقط (مش في التطبيق الحقيقي).

## اللي المفروض الاختبارات تغطيه دلوقتي
- `tests/test_auth.py`      → تسجيل حساب جديد، تسجيل الدخول، حماية صفحة الداشبورد.
- `tests/test_admin_access.py` → إن صفحات الأدمن بترفض أي حد مش مسجل دخول
  (تحويل لصفحة اللوجين) وأي مستخدم عادي (403)، وتسمح بس للأدمن.
- `tests/test_booking.py`   → حجز جديد، منع تكرار نفس الحجز لنفس التليفون
  في نفس المجموعة، وربط الحجز بالمستخدم لو داخل بحسابه.
- `tests/test_models.py`    → العلاقات بين الجداول (Booking ↔ Course ↔
  Schedule ↔ Branch) وبعض القيود الأساسية (unique / not null).

> ملاحظة: الاختبارات دي اتكتبت بمراجعة دقيقة لكود المشروع، لكن معنديش بيئة
> فيها اتصال إنترنت هنا عشان أنزّل المكتبات وأشغّلها فعليًا قبل ما أديهالك.
> شغّلها عندك، ولو ظهر أي فشل ابعتلي رسالة الخطأ وهصلحه فورًا.
